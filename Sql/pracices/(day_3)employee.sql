RENAME EMPLOYEE TO EMP;

ALTER TABLE EMP DROP COLUMN DNO;

UPDATE EMP
SET
    DNO=10
WHERE
    ID = 101;

UPDATE EMP
SET
    DNO=10
WHERE
    ID=102;

UPDATE EMP
SET
    DNO=20
WHERE
    ID=103;

UPDATE EMP
SET
    DNO=20
WHERE
    ID =104;

UPDATE EMP
SET
    DNO=30
WHERE
    ID= 105;

UPDATE EMP
SET
    DNO=30
WHERE
    ID = 501;

SELECT
    *
FROM
    EMP;

SELECT
    DNO,
    COUNT(*)
FROM
    EMP
GROUP BY
    DNO;

SELECT
    DNO,
    COUNT(*),
    SUM(ESALARY),
    AVG(ESALARY),
    MIN(ESALARY),
    MAX(ESALARY)
FROM
    EMP
GROUP BY
    DNO;

SELECT
    DNO,
    COUNT(*),
    SUM(ESALARY),
    AVG(ESALARY),
    MIN(ESALARY),
    MAX(ESALARY)
FROM
    EMP
GROUP BY
    DNO
HAVING
    MIN(ESALARY) > 2000
ORDER BY
    DNO;